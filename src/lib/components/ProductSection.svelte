<script>
  import * as m from "$lib/paraglide/messages";

  const products = [
    {
      imageURL: "/excitingpaddle.png",
      title: m.empty_fresh_frog_inspire(), // Anropa funktionen och tilldela resultatet
      description: m.empty_big_lionfish_flop(), // Anropa funktionen och tilldela resultatet
      price: m.soft_wide_quail_borrow(), // Anropa funktionen och tilldela resultatet
    },
    // Lägg till fler produkter om du vill
  ];
</script>
